<div class="container">
  <h2>WAT</h2>
  <% if current_user %>
  <h2>WAT</h2>
    <% User.all.each do |user|  %> 
      <p><%=user.name  %></p>
    <% end %>
  <% else %>
    <h1>Welcome!</h1>
  <% end %>
  
</div>
